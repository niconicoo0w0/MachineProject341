/**
 * vector
 * CS 341 - Spring 2023
 */
#include "sstring.h"

int main() {
    // TODO create some tests
    // sstring* c = cstr_to_sstring("abcdeeg");
    // sstring * v = sstring_substitute(c, 5, "ee", "ef"); // == [ "abcd", "", "fg" ]);
    // printf("%s\n", sstring_to_cstr(v));

    // c = cstr_to_sstring("abd");
    // v = sstring_substitute(c, 5, "ee", "ef"); // == [ "abcd", "", "fg" ]);
    // printf("%s\n", sstring_to_cstr(v));

    // c = cstr_to_sstring("1274830257");
    // v = sstring_substitute(c, 5, "ee", "ef"); // == [ "abcd", "", "fg" ]);
    // printf("%s\n", sstring_to_cstr(v));

    // c = cstr_to_sstring("{},{},{}");
    // v = sstring_substitute(c, 2, "{}", "31"); // == [ "abcd", "", "fg" ]);
    // printf("%s\n", sstring_to_cstr(v));

    // c = cstr_to_sstring("{},{},{}");
    // v = sstring_substitute(c, 1, "{}", "31"); // == [ "abcd", "", "fg" ]);
    // printf("%s\n", sstring_to_cstr(v));

    // c = cstr_to_sstring("{},{},{}");
    // v = sstring_substitute(c, 0, "{}", "31"); // == [ "abcd", "", "fg" ]);
    // printf("%s\n", sstring_to_cstr(v));


    return 0;
}
